# Normalize all text files to LF on commit
* text=auto eol=lf

# Explicitly declare binary files
*.jar binary
*.png binary
*.jpg binary
*.gif binary

# Gradle wrapper must stay executable on Unix
gradlew text eol=lf
