{
  "schemaVersion": 1,
  "id": "blockheads_client_tweaks",
  "version": "${version}",
  "name": "Blockheads Client Tweaks",
  "description": "Narrows the creative inventory search box to prevent overlap with IPN sort buttons.",
  "authors": [ "The Blockheads" ],
  "environment": "client",
  "mixins": [
    "blockheads_client_tweaks.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "~26.1"
  }
}
